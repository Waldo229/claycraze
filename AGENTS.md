# ClaycrazE / GENE Working Instructions

## Purpose

Keep the ClaycrazE website and GENE systems clear, restrained, reliable, and understandable. Apply KISS: make every signal legible enough that a child can understand it at some level.

## Authority

- Jim Alexander is the final human authority.
- GENE may witness, compare, discern, warn, and transmit.
- GENE reports `READY`, `READY WITH LIMITATIONS`, or `NOT READY`.
- Only Jim decides `LAUNCH` or `HOLD`.
- Do not commit, merge, publish, deploy, or overwrite production unless Jim explicitly requests it.

## GENE architecture

- Raspberry Pi (`gene`) witnesses.
- SiteGround remembers and publishes the common record.
- Cosmo reflects, compares, and performs Observatory or Study work.
- The firing—not each sensor fluctuation—is the unit of meaning.
- Separate observation from inference. Evidence is not authority.

## Language and character

- GENE means **Guardian of Emergent New Experiences**.
- Use non-gendered language for GENE.
- Prefer `PULSE` over `HEARTBEAT` and `AWARENESS` over `AROUSAL` in public interfaces.
- Preserve GENE's restrained, weathered, Sphinx-like voice. GENE speaks selectively.
- Humor may be brief and humane, but never obscure danger or interrupt urgent action.

## State display

GENE's visible firing state should progress through the approved character images as reliable temperature evidence crosses the configured thresholds. Current named states include:

1. Normal / Cool GENE
2. Chili GENE
3. Safety Engineer GENE (quartz-inversion region)
4. Atomic GENE
5. Higher-temperature state(s), including ThermoNuclear GENE where configured

Do not invent or silently change thresholds. Locate the existing canonical thresholds and image filenames before modifying state logic. Stale or invalid probe data must not continue to drive a hot state.

## Protected scope

- Preserve the current introductory prototype pages unless Jim explicitly reopens that work.
- Do not change Ikebana or unrelated GENE content while repairing another feature.
- Preserve existing user work and unrelated repository changes.
- Prefer the smallest repair that fixes the verified fault.

## Work record

Before editing, read `CURRENT_TASK.md`. After testing, update it with findings, files changed, verification results, and the next safe step. Record completed work in `CHANGELOG.md`.
